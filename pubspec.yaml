name: providerclass
description: "A new Flutter project."

publish_to: 'none' 
version: 0.1.0

environment:
  sdk: '>=3.5.3 


dev_dependencies:
  flutter_lints: ^2.0.0
  build_runner: ^2.1.10
  build_test: ^2.1.10
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  window_size: ^0.3.0
